Testing some of the Go features

This program will get some parameters of a projectile and will trace its deslocation
I used a coordinate system with X and Y axis
Give it a start X and Y position, a velocity vector, an angle in degrees and a duration
The duration ticks every 1 second